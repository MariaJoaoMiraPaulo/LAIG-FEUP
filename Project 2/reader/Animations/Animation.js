/**
* Animation
* @param id id of the object
* @param animationTime the time that the animation will take
* @constructor
*/
class Animation {
  constructor(scene,id, animationTime){
    this.scene = scene;
    this.id = id;
    this.animationTime = animationTime;
  }
}
